import { ListClassDirective } from './list-class.directive';

describe('ListClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ListClassDirective();
    expect(directive).toBeTruthy();
  });
});
